(() => {
  const DATA_URL = "https://pvp-data.warmycat.com/guildwar_ranking.json";
  const COMPACT_URLS = {
    LEGEND: "https://pvp-data.warmycat.com/guildwar_data_LEGEND.json",
    MASTER: "https://pvp-data.warmycat.com/guildwar_data_MASTER.json",
    DIAMOND: "https://pvp-data.warmycat.com/guildwar_data_DIAMOND.json",
  };
  const RANGERS_URL = "../../res/Rangers_data.json";
  const ID_DICT_URL = "../../res/id_dict.json";
  const ABILITY_URL = "../../res/%E8%83%BD%E5%8A%9B.json";
  const RANGER_IMAGE = id => `https://rangers.lerico.net/res/${encodeURIComponent(id)}/${encodeURIComponent(id)}-thum.png`;
  const GEAR_ICON = id => `https://rangers.lerico.net/res/gear_icon/${encodeURIComponent(id)}_icon.png`;
  const ABILITY_ICON = icon => `https://rangers.lerico.net/res/ability_icon/${encodeURIComponent(icon)}`;
  const TALENT_ICON = grade => `../../assets/tlt_icon/tlt${encodeURIComponent(grade)}.png`;
  const NONE_CODE = "__NONE__";
  const SLOT_LABELS = { WEAPON:"武器", ARMOR:"防具", ACC:"飾品" };
  const ADMIN_MODE = localStorage.getItem("rangerbook-admin-mode") === "true";
  const TIERS = {
    LEGEND: { label:"傳奇", min:1, max:50 },
    MASTER: { label:"大師", min:51, max:200 },
    DIAMOND: { label:"鑽石", min:201, max:400 },
  };
  const els = {
    updated:document.getElementById("guildRankingUpdated"), count:document.getElementById("guildRankingCount"), tier:document.getElementById("guildRankingTier"), tierLabel:document.getElementById("guildRankingTierLabel"), search:document.getElementById("guildRankingSearch"), status:document.getElementById("guildRankingStatus"), body:document.getElementById("guildRankingBody"), modal:document.getElementById("guildMemberModal"), modalContent:document.getElementById("guildMemberModalContent"), modalClose:document.getElementById("guildMemberModalClose"),
  };
  let guilds=[], rangerNames={}, gearNames={}, abilityMap={}, openGuildRank=0, selectedMemberIndex=-1;
  const tierDataCache=new Map();
  const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
  const num=v=>Number.isFinite(Number(v))?Number(v).toLocaleString("zh-Hant",{maximumFractionDigits:2}):"-";
  const date=v=>{const d=new Date(v);return Number.isNaN(d.getTime())?"-":new Intl.DateTimeFormat("zh-Hant",{year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false}).format(d)};
  const status=(text="",error=false)=>{if(!els.status)return;els.status.hidden=!text;els.status.textContent=text;els.status.classList.toggle("error",error)};
  const currentTierCode=()=>els.tier?.value||"LEGEND";
  const currentTier=()=>TIERS[currentTierCode()]||TIERS.LEGEND;
  const tierGuilds=()=>{const t=currentTier();return guilds.filter(g=>Number(g.rank)>=t.min&&Number(g.rank)<=t.max)};
  const memberName=m=>m?.displayName||m?.name||m?.playerName||"未公開名稱";
  const memberRawLevel=m=>Number(m?.level??m?.playerLevel??0)||0;
  const memberLevel=m=>{const raw=m?.displayLevel??m?.level??m?.playerLevel;return raw===undefined||raw===null||raw===""?"-":num(raw)};

  function levelBadgeFile(rawLevel){const value=Number(rawLevel);if(!Number.isFinite(value))return"";if(value>=100&&value<=198)return"level_badge_master.png";if(value>=199&&value<=297)return"level_badge_super.png";if(value>=298&&value<=396)return"level_badge_ultra.png";if(value>=397&&value<=495)return"level_badge_legend.png";if(value>=496&&value<=594)return"level_badge_superlegend.png";return""}
  function memberLevelHtml(member){const badgeFile=levelBadgeFile(memberRawLevel(member));const badge=badgeFile?`<img class="pvp-level-badge" src="../../assets/level_icon/${badgeFile}" alt="" aria-hidden="true">`:"";return `<span class="pvp-player-level-line">${badge}<span>Lv. ${esc(memberLevel(member))}</span></span>`}

  function render(){const q=(els.search?.value||"").trim().toLowerCase(),tier=currentTier(),scoped=tierGuilds(),rows=scoped.filter(g=>!q||String(g.guildName||"").toLowerCase().includes(q));if(els.tierLabel)els.tierLabel.textContent=tier.label;if(els.count)els.count.textContent=num(scoped.length);if(!els.body)return;els.body.innerHTML=rows.length?rows.map(g=>`<tr class="guildwar-rank-row" data-guild-rank="${esc(g.rank)}" tabindex="0" role="button"><td class="pvp-rank-cell"><span class="pvp-rank-medal">${esc(g.rank)}</span></td><td><strong title="${esc(g.guildName||"-")}">${esc(g.guildName||"-")}</strong></td><td>${num(g.score)}</td><td>${num(g.curMemberCount)} / ${num(g.maxMemberCount)}</td><td>${esc(g.nationalFlag||"-")}</td></tr>`).join(""):`<tr class="pvp-empty-row"><td colspan="5">目前沒有${esc(tier.label)}段位的公會資料。</td></tr>`}

  async function loadTierData(code){if(tierDataCache.has(code))return tierDataCache.get(code);const url=COMPACT_URLS[code];if(!url)return{};const promise=fetch(`${url}?t=${Date.now()}`,{cache:"no-store"}).then(r=>r.ok?r.json():{}).catch(()=>({}));tierDataCache.set(code,promise);return promise}
  function compactGuild(data,rank){return (Array.isArray(data?.guilds)?data.guilds:[]).find(g=>Number(g.rank)===Number(rank))||null}
  function normalizeUnits(value){if(Array.isArray(value))return value.filter(x=>x&&typeof x==="object"&&x.unitCode);if(!value||typeof value!=="object")return[];if(value.unitCode)return[value];return Object.keys(value).sort((a,b)=>Number(a)-Number(b)||String(a).localeCompare(String(b))).flatMap(k=>normalizeUnits(value[k]))}
  function normalizeTeamGroups(guildwar){if(!guildwar||typeof guildwar!=="object")return[];return Object.entries(guildwar).map(([k,v])=>[k,normalizeUnits(v)]).filter(([,u])=>u.length)}
  function rangerName(code){return rangerNames[code]||code||"未知角色"}
  function unitLevel(unit){const value=[unit?.level,unit?.unitLevel,unit?.unitLv,unit?.rangerLevel].find(v=>v!==undefined&&v!==null&&v!=="");return value===undefined?"-":num(value)}
  function equipmentCode(unit,slot){const map=unit?.equipMap&&typeof unit.equipMap==="object"?unit.equipMap:(unit?.equipment||{}),value=map?.[slot];if(!value)return NONE_CODE;if(typeof value==="string")return value||NONE_CODE;if(typeof value==="object")return String(value.equipItemCode||value.itemCode||value.code||NONE_CODE);return NONE_CODE}
  function equipmentItem(unit,slot){const code=equipmentCode(unit,slot),isNone=!code||code===NONE_CODE,name=isNone?"未裝備":(gearNames[code]||code),icon=isNone?`<span class="pvp-player-equipment-empty-icon" aria-hidden="true">—</span>`:`<img src="${GEAR_ICON(code)}" alt="" loading="lazy" onerror="this.remove();">`;return `<div class="pvp-player-equipment-item">${icon}<div><span>${SLOT_LABELS[slot]}</span><strong title="${esc(name)}">${esc(name)}</strong></div></div>`}
  function abilityInfo(unit){const code=String(unit?.awakeAbilityCode||"").trim();if(!code)return{name:"未設定覺醒能力",icon:""};const info=abilityMap[code]||{};return{name:info["名稱"]||code,icon:info.icon||String(unit?.awakeAbilityIcon||"").trim()}}
  function talentInfo(unit){const raw=unit?.talentGrade;if(raw===undefined||raw===null||raw==="")return{name:"才能解放狀態無資料",icon:"",badge:"?"};const grade=Number(raw);if(!Number.isInteger(grade)||grade<0||grade>4)return{name:`才能解放階段 ${raw}`,icon:"",badge:String(raw)};return{name:grade===0?"未解放才能":`才能解放階段 ${grade}`,icon:TALENT_ICON(grade),badge:String(grade)}}
  function extraDetailItem(label,value,icon="",badge=""){const iconHtml=icon?`<img class="pvp-player-extra-icon" src="${icon}" alt="" loading="lazy" onerror="this.remove();">`:`<span class="pvp-player-extra-icon pvp-player-extra-icon-empty" aria-hidden="true">${esc(badge||"—")}</span>`;return `<div class="pvp-player-extra-item">${iconHtml}<div><span>${esc(label)}</span><strong>${esc(value)}</strong></div></div>`}
  function renderUnitDetail(unit){const target=document.getElementById("guildwarPlayerUnitDetail");if(!target)return;if(!unit){target.innerHTML=`<div class="pvp-player-unit-detail-empty">點擊左側角色查看等級、覺醒能力、解放才能與裝備。</div>`;return}const code=String(unit.unitCode||""),ability=abilityInfo(unit),talent=talentInfo(unit),abilityIcon=ability.icon?ABILITY_ICON(ability.icon):"";target.innerHTML=`<div class="pvp-player-unit-detail-card"><div class="pvp-player-unit-detail-head"><img src="${RANGER_IMAGE(code)}" alt="" loading="lazy" onerror="this.remove();"><div><strong>${esc(rangerName(code))}</strong><span>等級：Lv. ${esc(unitLevel(unit))}</span></div></div><div class="pvp-player-extra-list">${extraDetailItem("覺醒能力",ability.name,abilityIcon)}${extraDetailItem("解放才能",talent.name,talent.icon,talent.badge)}</div><div class="pvp-player-equipment-list">${equipmentItem(unit,"WEAPON")}${equipmentItem(unit,"ARMOR")}${equipmentItem(unit,"ACC")}</div></div>`}

  async function renderGuildMembers(guild){els.modalContent.innerHTML=`<div class="guildwar-member-empty">公會成員資料載入中…</div>`;const data=await loadTierData(currentTierCode()),detail=compactGuild(data,guild.rank),source=Array.isArray(detail?.members)?detail.members:[];const members=source.map((member,index)=>({member,index})).sort((a,b)=>memberRawLevel(b.member)-memberRawLevel(a.member)||memberName(a.member).localeCompare(memberName(b.member),"zh-Hant"));const list=members.length?`<div class="guildwar-member-list">${members.map(({member:m,index})=>`<div class="guildwar-member-row"><div><div class="guildwar-member-name">${esc(memberName(m))}</div><div class="guildwar-member-level">${memberLevelHtml(m)}</div></div>${ADMIN_MODE?`<button class="guildwar-member-team-button" type="button" data-member-team-index="${index}">查看進攻隊伍</button>`:""}</div>`).join("")}</div>`:`<div class="guildwar-member-empty">此公會的成員資料尚未產生。</div>`;els.modalContent.innerHTML=`<header class="guildwar-member-header"><h2 id="guildMemberModalTitle">${esc(guild.guildName||"-")}</h2><p>排名第 ${num(guild.rank)} 名 · ${num(guild.curMemberCount)} / ${num(guild.maxMemberCount)} 名成員</p></header>${list}`}

  async function renderMemberTeam(index){const guild=guilds.find(g=>Number(g.rank)===openGuildRank);if(!guild)return;const data=await loadTierData(currentTierCode()),detail=compactGuild(data,openGuildRank),member=Array.isArray(detail?.members)?detail.members[index]:null;if(!member)return;selectedMemberIndex=index;const groups=normalizeTeamGroups(member.guildwar),units=groups.flatMap(([,items])=>items);const content=units.length?`<div class="pvp-player-team-layout"><section class="pvp-player-team-pane"><h3>隊伍角色</h3><div id="guildwarPlayerTeamGrid" class="pvp-player-team-grid">${units.map((unit,unitIndex)=>{const code=String(unit.unitCode||"");return `<button class="pvp-player-unit-button" type="button" data-guildwar-unit-index="${unitIndex}" title="${esc(rangerName(code))}"><img class="pvp-player-unit-image" src="${RANGER_IMAGE(code)}" alt="" loading="lazy" onerror="this.remove();"><span class="pvp-player-unit-name">${esc(rangerName(code))}</span></button>`}).join("")}</div></section><aside class="pvp-player-unit-detail"><h3>角色詳細資料</h3><div id="guildwarPlayerUnitDetail"></div></aside></div>`:`<div class="guildwar-member-empty">此成員目前沒有可顯示的公會戰進攻隊伍資料。</div>`;els.modalContent.innerHTML=`<button class="guildwar-team-back" type="button" data-team-back>← 返回成員列表</button><header class="guildwar-member-header"><h2 id="guildMemberModalTitle">${esc(memberName(member))}</h2><p>${esc(guild.guildName||"-")} · Lv. ${esc(memberLevel(member))}</p></header>${content}`;if(units.length){els.modalContent.dataset.currentUnits=JSON.stringify(units.map((_,i)=>i));window.__guildwarCurrentUnits=units;renderUnitDetail(null)}}

  function openGuild(guild){if(!guild||!els.modal)return;openGuildRank=Number(guild.rank)||0;selectedMemberIndex=-1;els.modal.hidden=false;document.body.classList.add("modal-open");renderGuildMembers(guild)}
  function closeModal(){if(!els.modal||els.modal.hidden)return;els.modal.hidden=true;document.body.classList.remove("modal-open");window.__guildwarCurrentUnits=null}
  function guildFromRow(row){const rank=Number(row?.dataset?.guildRank);return guilds.find(g=>Number(g.rank)===rank)}
  async function optional(url,fallback){try{const r=await fetch(url);return r.ok?r.json():fallback}catch{return fallback}}
  async function load(){status("公會排名資料載入中…");try{const requests=[fetch(`${DATA_URL}?t=${Date.now()}`,{cache:"no-store"})];if(ADMIN_MODE)requests.push(optional(RANGERS_URL,[]),optional(ID_DICT_URL,{}),optional(ABILITY_URL,{}));const results=await Promise.all(requests),rankingRes=results[0];if(!rankingRes.ok)throw new Error(`HTTP ${rankingRes.status}`);const data=await rankingRes.json();guilds=Array.isArray(data.guilds)?data.guilds:[];if(ADMIN_MODE){rangerNames={};(Array.isArray(results[1])?results[1]:[]).forEach(row=>{const code=String(row.ranger_id||"");if(code)rangerNames[code]=String(row["Ranger名稱"]||code)});gearNames=Object.fromEntries(Object.entries(results[2]||{}).map(([name,code])=>[String(code),String(name)]));abilityMap=results[3]&&typeof results[3]==="object"?results[3]:{}}if(els.updated)els.updated.textContent=date(data.metadata?.generatedAtUtc);status();render()}catch(e){console.error(e);status("公會排名資料尚未產生或目前無法載入。",true);if(els.body)els.body.innerHTML=""}}

  els.search?.addEventListener("input",render);els.tier?.addEventListener("change",render);els.body?.addEventListener("click",e=>{const row=e.target.closest("[data-guild-rank]");if(row)openGuild(guildFromRow(row))});els.body?.addEventListener("keydown",e=>{if(e.key!=="Enter"&&e.key!==" ")return;const row=e.target.closest("[data-guild-rank]");if(row){e.preventDefault();openGuild(guildFromRow(row))}});els.modalContent?.addEventListener("click",e=>{const memberButton=e.target.closest("[data-member-team-index]");if(memberButton&&ADMIN_MODE){renderMemberTeam(Number(memberButton.dataset.memberTeamIndex));return}const unitButton=e.target.closest("[data-guildwar-unit-index]");if(unitButton&&ADMIN_MODE){const units=window.__guildwarCurrentUnits||[],unit=units[Number(unitButton.dataset.guildwarUnitIndex)];if(unit){els.modalContent.querySelectorAll("[data-guildwar-unit-index]").forEach(b=>b.classList.toggle("is-active",b===unitButton));renderUnitDetail(unit)}return}if(e.target.closest("[data-team-back]")){const guild=guilds.find(g=>Number(g.rank)===openGuildRank);if(guild)renderGuildMembers(guild)}});els.modalClose?.addEventListener("click",closeModal);els.modal?.addEventListener("click",e=>{if(e.target.closest("[data-guild-member-modal-close]"))closeModal()});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});load();
})();