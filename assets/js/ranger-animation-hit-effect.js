(()=>{const SITE_ROOT=window.location.pathname.includes("/rangerbook/")?"/rangerbook/":"/";const ANIMATION_META_BASE="https://res.warmycat.com/animation_meta/";const ANIMATION_INDEX_URL=`${ANIMATION_META_BASE}index.json`;const PROJECTILE_DATA_URL=`${SITE_ROOT}res/projectile_data.json`;const HIT_EFFECT_BASE=`${SITE_ROOT}assets/hit_effect/`;const HIT_EFFECT_META_URL=`${HIT_EFFECT_BASE}eff_hit_strong_pretty.json`;const HIT_EFFECT_SEGMENT="basic";const HIT_EFFECT_DURATION=19/30;const NATIVE_ACTION_FPS=60;const NATIVE_PROJECTILE_COORDINATE_SCALE=0.5;const NORMAL_STAGE_INITIAL_SCALE=0.85;const VIEWER_RESOURCE_SCALE=2.28;const MIN_FLIGHT_DURATION=1/NATIVE_ACTION_FPS;const MAX_FLIGHT_DURATION=8;const RANGER_X_RATIO=0.50;const TARGET_X_RATIO=0.90;const TARGET_DISTANCE_MULTIPLIER=2.25;const GROUND_Y_RATIO=0.80;const BODY_OFFSET_X=-130;const BODY_OFFSET_Y=-88;const CLIPS=[{key:"idle",body:["idle","wait"]},{key:"move",body:["walk"]},{key:"knockback",body:["knockback"]},{key:"attack",dataKey:"normal",hitRateKey:"normalHitPointRate",body:["attack_all","attack","attack_a","attack_b"],ready:["attack_ready"],trigger:["attack","attack_a","attack_b"],bullet:"bul",isBasicAttack:true,},{key:"skill1",dataKey:"skill1",hitRateKey:"skill1HitPointRate",body:["s_attack_all","s_action_attack_all","s_attack","s_attack_a","s_attack_b","s_action_attack_2","s_action_attack_3"],ready:["s_attack_ready","s_action_attack_1"],trigger:["s_attack","s_attack_a","s_attack_b","s_action_attack_2","s_action_attack_3"],bullet:"bul2",isBasicAttack:false,},{key:"skill2",dataKey:"skill2",hitRateKey:"skill2HitPointRate",body:["s2_attack_all","s2_attack","s2_attack_a","s2_attack_b","skill"],ready:["s2_attack_ready"],trigger:["s2_attack","s2_attack_a","s2_attack_b","skill"],bullet:"bul3",isBasicAttack:false,},{key:"full",sequence:["move","idle","attack","skill1","skill2","knockback"]},];const sectionStates=new WeakMap();const metaCache=new Map();const imageCache=new Map();let indexPromise=null;let projectileDataPromise=null;let effectMetaPromise=null;let patchScheduled=false;function text(value){return value===null||value===undefined?"":String(value).trim();}function finiteNumber(value,fallback=0){const number=Number(value);return Number.isFinite(number)?number:fallback;}function positiveNumber(value,fallback=0){const number=finiteNumber(value,fallback);return number>0?number:fallback;}function clamp(value,minimum,maximum){return Math.min(maximum,Math.max(minimum,value));}function loadJson(url){return fetch(url).then((response)=>(response.ok?response.json():null)).catch(()=>null);}function loadIndex(){if(!indexPromise)indexPromise=loadJson(ANIMATION_INDEX_URL);return indexPromise;}function loadProjectileData(){if(!projectileDataPromise)projectileDataPromise=loadJson(PROJECTILE_DATA_URL);return projectileDataPromise;}function loadEffectMeta(){if(!effectMetaPromise)effectMetaPromise=loadJson(HIT_EFFECT_META_URL);return effectMetaPromise;}function loadEffectImage(filename){const name=text(filename);if(!name)return Promise.resolve(null);if(imageCache.has(name))return imageCache.get(name);const promise=new Promise((resolve,reject)=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>reject(new Error(`Hit effect image failed: ${name}`));image.src=`${HIT_EFFECT_BASE}${encodeURIComponent(name)}`;}).catch(()=>null);imageCache.set(name,promise);return promise;}function attachProjectileDataFallback(meta,projectileData,unitId){if(!meta||meta.projectileData)return meta;const unitData=projectileData?.units?.[unitId];if(!unitData)return meta;meta.projectileData={schemaVersion:projectileData?.schemaVersion??null,motionModelVersion:projectileData?.validation?.motionModelVersion??null,coordinateScale:NATIVE_PROJECTILE_COORDINATE_SCALE,unitId,render:unitData.render||null,movement:unitData.movement||null,hitTiming:unitData.hitTiming||null,bullet:unitData.bullet||null,normal:unitData.normal||null,skill1:unitData.skill1||null,skill2:unitData.skill2||null,effects:unitData.effects||null,sounds:unitData.sounds||null,};return meta;}async function loadUnitMeta(unitId){if(!unitId)return null;if(metaCache.has(unitId))return metaCache.get(unitId);const promise=Promise.all([loadIndex(),loadProjectileData()]).then(async([index,projectileData])=>{const entry=index?.units?.[unitId];if(!entry?.meta)return null;const filename=text(entry.meta).split("/").pop()||`${unitId}.json`;const meta=await loadJson(`${ANIMATION_META_BASE}${encodeURIComponent(filename)}`);return attachProjectileDataFallback(meta,projectileData,unitId);});metaCache.set(unitId,promise);return promise;}function getAnim(part,names){if(!part)return null;for(const name of names||[]){const animation=part.animations?.[name];if(animation?.frames?.length)return{name,anim:animation};}return null;}function animDuration(part,animationResult){if(!animationResult)return 0;return animationResult.anim.frame_count/Math.max(1,part?.anim_rate||24);}function namedAnimationDuration(part,name){const animation=part?.animations?.[name];return animation?.frame_count?animation.frame_count/Math.max(1,part.anim_rate||24):0;}function nativeProjectileSpawnTime(bodyPart,bodyAnimName,clip,clipDuration){const virtualClip=bodyPart?.virtual_clips?.[bodyAnimName];if(Array.isArray(virtualClip?.segments)){let cursor=0;for(const segmentName of virtualClip.segments){if((clip.trigger||[]).includes(segmentName)){return clamp(cursor,0,Math.max(0,clipDuration-0.001));}cursor+=namedAnimationDuration(bodyPart,segmentName);}}if((clip.trigger||[]).includes(bodyAnimName))return 0;if(bodyAnimName==="_all"&&Array.isArray(bodyPart?.timeline?.labels)){const label=bodyPart.timeline.labels.find((item)=>(clip.trigger||[]).includes(item.name));if(label)return clamp(finiteNumber(label.seconds,0),0,Math.max(0,clipDuration-0.001));}for(const readyName of clip.ready||[]){const duration=namedAnimationDuration(bodyPart,readyName);if(duration>0)return clamp(duration,0,Math.max(0,clipDuration-0.001));}return 0;}function spriteDims(part,imageName){const sprite=part?.sprites?.[imageName];if(!sprite)return null;const[,,width,height]=sprite.rect||[];if(!width||!height)return null;return{w:width,h:height};}function worldPosition(part,item){const[,resourceNumber,objectMatrix,color]=item||[];const imageDefinition=part?.images?.[resourceNumber];if(!imageDefinition||!Array.isArray(objectMatrix)||!Array.isArray(imageDefinition.m))return null;const dimensions=spriteDims(part,imageDefinition.name);if(!dimensions)return null;const[m00,m01,m10,m11,m02,m12]=objectMatrix;const[i00,i01,i10,i11,i02,i12]=imageDefinition.m;const centerX=dimensions.w*0.5;const centerY=dimensions.h*0.5;const imageCenterX=i00*centerX+i01*centerY+i02;const imageCenterY=i10*centerX+i11*centerY+i12;return{x:m00*imageCenterX+m01*imageCenterY+m02,y:m10*imageCenterX+m11*imageCenterY+m12,color,};}function frontMostPoint(part,frame){let best=null;for(const item of frame||[]){const color=item?.[3];const alpha=Array.isArray(color)?Number(color[3]??255):255;if(alpha===0)continue;const point=worldPosition(part,item);if(point&&(!best||point.x>best.x))best=point;}return best;}function isInjectedProjectileMarker(item){const markerId=Number(item?.[0]);return Number.isFinite(markerId)&&markerId>=2147483000&&markerId<2147484000;}function animationMarkerMuzzle(part,readyNames,triggerNames,includeInjected=true){const ready=getAnim(part,readyNames);const trigger=getAnim(part,triggerNames);if(!ready||!trigger)return null;const readyFrames=ready.anim.frames||[];const triggerFrames=trigger.anim.frames||[];if(!readyFrames.length||!triggerFrames.length)return null;const lastReady=readyFrames[readyFrames.length-1]||[];const firstTrigger=triggerFrames[0]||[];const hidden=lastReady.filter((item)=>{const color=item?.[3];const hiddenItem=Array.isArray(color)&&Number(color[3]??255)===0;return hiddenItem&&(includeInjected||!isInjectedProjectileMarker(item));});const marker=hidden.find((candidate)=>firstTrigger.some((item)=>item?.[0]===candidate?.[0]&&item?.[1]===candidate?.[1]))||hidden.find((candidate)=>firstTrigger.some((item)=>item?.[1]===candidate?.[1]));return marker?worldPosition(part,marker):null;}function animationDerivedMuzzle(part,readyNames,triggerNames){const ready=getAnim(part,readyNames);if(!ready)return null;const readyFrames=ready.anim.frames||[];if(!readyFrames.length)return null;return animationMarkerMuzzle(part,readyNames,triggerNames,true)||frontMostPoint(part,readyFrames[readyFrames.length-1]||[]);}function normalizeMotionType(projectileConfig){const explicit=text(projectileConfig?.motion?.type).toUpperCase();if(["LINEAR","CURVE","RETURN","BEAM","DIRECT","NONE","UNKNOWN"].includes(explicit))return explicit;const attackType=text(projectileConfig?.attackType).toUpperCase();if(["ENERGY","WEAPON","DOUBLE"].includes(attackType))return"LINEAR";if(["ENERGYC","WEAPONC","DOUBLEC"].includes(attackType))return"CURVE";if(attackType==="RETURN")return"RETURN";if(attackType==="BEAM")return"BEAM";if(["LASER","ACTION"].includes(attackType))return"SPECIAL";if(["PUNCH","KICK","SWING","STAB"].includes(attackType))return"DIRECT";if(!attackType||attackType==="NONE")return"NONE";return"UNKNOWN";}function defaultHitPointRate(motionType){if(motionType==="CURVE")return 0;if(["LINEAR","RETURN"].includes(motionType))return 0.25;return null;}function effectiveHitPointRate(meta,clip,motionType){const raw=finiteNumber(meta?.projectileData?.hitTiming?.[clip.hitRateKey],NaN);if(!Number.isFinite(raw))return defaultHitPointRate(motionType);return clamp(raw>1?raw/100:raw,0,1);}function nativeStartPoint(meta,clip,projectileConfig,bodyPart){const coordinateScale=positiveNumber(meta?.projectileData?.coordinateScale,NATIVE_PROJECTILE_COORDINATE_SCALE);const rawX=finiteNumber(projectileConfig?.start?.x,NaN);const rawY=finiteNumber(projectileConfig?.start?.y,NaN);if(Number.isFinite(rawX)&&Number.isFinite(rawY)){return{x:rawX*coordinateScale,y:rawY*coordinateScale,source:"database"};}const authored=animationMarkerMuzzle(bodyPart,clip.ready,clip.trigger,false);if(authored)return{x:authored.x,y:authored.y,source:"animation-anchor"};const fallback=animationDerivedMuzzle(bodyPart,clip.ready,clip.trigger);if(fallback)return{x:fallback.x,y:fallback.y,source:"animation-fallback"};return{x:0,y:0,source:"database"};}function movementDuration(distance,moveSpeed){const speed=positiveNumber(moveSpeed,0);if(!speed)return 0;return clamp(distance/(speed*NATIVE_ACTION_FPS),MIN_FLIGHT_DURATION,MAX_FLIGHT_DURATION);}function targetSceneDistance(width,zoom){return width*(TARGET_X_RATIO-RANGER_X_RATIO)*TARGET_DISTANCE_MULTIPLIER*zoom;}function selectedTargetProfile(section,bodyPart){const profile=window.RangerAnimationTargetBridge?.get(section);return{contentHeight:positiveNumber(profile?.contentHeight,positiveNumber(bodyPart?.canvas?.h,240)),};}function referenceLayout(){const width=640;const height=360;const zoom=1;const baseSceneScale=Math.min(width/1400,height/750)*VIEWER_RESOURCE_SCALE*NORMAL_STAGE_INITIAL_SCALE;const sceneScale=baseSceneScale*zoom;const actorX=width*RANGER_X_RATIO;const actorY=height*GROUND_Y_RATIO;return{actorX,actorY,targetX:actorX+targetSceneDistance(width,zoom),targetBaseY:actorY,bodyOriginX:actorX+BODY_OFFSET_X*sceneScale,bodyOriginY:actorY+BODY_OFFSET_Y*sceneScale,sceneScale,facing:1,};}function resolveStartScreen(projectile,layout,sceneScale){return{x:layout.bodyOriginX+layout.facing*projectile.start.x*sceneScale,y:layout.bodyOriginY+(projectile.start.source==="database"?-projectile.start.y:projectile.start.y)*sceneScale,};}function resolveReturnEnd(projectile,layout,sceneScale){return{x:layout.bodyOriginX+layout.facing*projectile.secondStart.x*sceneScale,y:layout.bodyOriginY-projectile.secondStart.y*sceneScale,};}function resolveOutboundEnd(section,projectile,bodyPart,layout,sceneScale){const targetHeight=selectedTargetProfile(section,bodyPart).contentHeight;const targetHitHeight=targetHeight*finiteNumber(projectile.hitPointRate,0);return{x:layout.targetX+layout.facing*projectile.endOffset.x*sceneScale,y:layout.targetBaseY-(targetHitHeight+projectile.endOffset.y)*sceneScale,};}function resolveProjectileGeometry(section,projectile,bodyPart){const layout=referenceLayout();const sceneScale=layout.sceneScale;const start=resolveStartScreen(projectile,layout,sceneScale);const end=resolveOutboundEnd(section,projectile,bodyPart,layout,sceneScale);const nativeDistance=Math.hypot(end.x-start.x,end.y-start.y)/Math.max(0.0001,sceneScale);const flightDuration=movementDuration(nativeDistance,projectile.moveSpeed);const returnEnd=resolveReturnEnd(projectile,layout,sceneScale);const returnNativeDistance=Math.hypot(returnEnd.x-end.x,returnEnd.y-end.y)/Math.max(0.0001,sceneScale);return{flightDuration,returnDuration:movementDuration(returnNativeDistance,projectile.moveSpeed),};}function projectileNormalEnd(section,projectile,bodyPart){const geometry=resolveProjectileGeometry(section,projectile,bodyPart);const nativeMotionAvailable=geometry.flightDuration>0;const renderMode=nativeMotionAvailable?projectile.renderMode:"LEGACY";if(renderMode==="BEAM")return projectile.beamDuration;if(renderMode==="DIRECT")return projectile.localNormalDuration;if(renderMode==="RETURN")return geometry.flightDuration;if(["LINEAR","CURVE"].includes(renderMode))return geometry.flightDuration;return Math.max(projectile.localNormalDuration,15/Math.max(1,bodyPart?.anim_rate||24));}function estimateProjectileLifetime(section,projectile,bodyPart){const geometry=resolveProjectileGeometry(section,projectile,bodyPart);if(projectile.renderMode==="RETURN"&&geometry.flightDuration>0){return geometry.flightDuration+geometry.returnDuration;}if(projectile.renderMode==="BEAM")return projectile.beamDuration+projectile.finishDuration;if(projectile.renderMode==="DIRECT")return projectile.localNormalDuration+projectile.finishDuration;if(["LINEAR","CURVE"].includes(projectile.renderMode)&&geometry.flightDuration>0){return geometry.flightDuration+projectile.finishDuration;}return Math.max(projectile.localNormalDuration,15/Math.max(1,bodyPart?.anim_rate||24))+projectile.finishDuration;}function buildProjectile(section,meta,clip,bodyPart,bodyAnimation,clipDuration){if(!clip.bullet)return null;const projectileConfig=meta?.projectileData?.[clip.dataKey]||null;const motionType=normalizeMotionType(projectileConfig);if(motionType==="NONE")return null;const requestedPartName=text(projectileConfig?.animationPart)||clip.bullet;const bulletPart=meta?.parts?.[requestedPartName]||meta?.parts?.[clip.bullet];if(!bulletPart)return null;const standardNormal=getAnim(bulletPart,["normal","idle","wait","shot","fire","attack","_all"]);const outboundAnimation=motionType==="RETURN"?(getAnim(bulletPart,["normal_a"])||standardNormal):standardNormal;if(!outboundAnimation)return null;const returnAnimation=motionType==="RETURN"?(getAnim(bulletPart,["normal_b"])||outboundAnimation):null;const finishAnimation=getAnim(bulletPart,["finish","hit","end"]);const spawnTime=nativeProjectileSpawnTime(bodyPart,bodyAnimation.name,clip,clipDuration);const coordinateScale=positiveNumber(meta?.projectileData?.coordinateScale,NATIVE_PROJECTILE_COORDINATE_SCALE);const configuredEnabled=projectileConfig?.motion?.enabled;let renderMode=motionType;if(motionType==="SPECIAL"||motionType==="UNKNOWN")renderMode="LEGACY";if(configuredEnabled===false&&!["DIRECT","SPECIAL"].includes(motionType))renderMode="LEGACY";const beamLength=positiveNumber(meta?.projectileData?.bullet?.length,0);const beamDuration=positiveNumber(meta?.projectileData?.bullet?.duration,0);if(renderMode==="BEAM"&&(!beamLength||!beamDuration))renderMode="LEGACY";const projectile={partName:bulletPart===meta?.parts?.[requestedPartName]?requestedPartName:clip.bullet,motionType,renderMode,normalAnimName:outboundAnimation.name,returnAnimName:returnAnimation?.name||outboundAnimation.name,finishAnimName:finishAnimation?.name||"",spawnTime,start:nativeStartPoint(meta,clip,projectileConfig,bodyPart),secondStart:{x:finiteNumber(projectileConfig?.secondStart?.x,finiteNumber(projectileConfig?.start?.x,0))*coordinateScale,y:finiteNumber(projectileConfig?.secondStart?.y,finiteNumber(projectileConfig?.start?.y,0))*coordinateScale,},endOffset:{x:finiteNumber(projectileConfig?.end?.x,0)*coordinateScale,y:finiteNumber(projectileConfig?.end?.y,0)*coordinateScale,},moveSpeed:positiveNumber(projectileConfig?.moveSpeed,0),hitPointRate:effectiveHitPointRate(meta,clip,motionType),localNormalDuration:Math.max(1,outboundAnimation.anim.frame_count||0)/Math.max(1,bulletPart.anim_rate||24),finishDuration:finishAnimation?animDuration(bulletPart,finishAnimation):0,beamLength,beamDuration,};projectile.normalEnd=projectileNormalEnd(section,projectile,bodyPart);projectile.lifetime=estimateProjectileLifetime(section,projectile,bodyPart);return projectile;}function computeTrack(section,meta,clipKey){const clip=CLIPS.find((item)=>item.key===clipKey)||CLIPS[0];if(clip.sequence){const segments=[];let cursor=0;for(const childKey of clip.sequence){const childTrack=computeTrack(section,meta,childKey);if(!childTrack.segments.length)continue;for(const segment of childTrack.segments){segments.push({...segment,start:(segment.start||0)+cursor});}cursor+=childTrack.duration||0;}return{key:clip.key,segments,duration:cursor||1};}const bodyPart=meta?.parts?.body;const bodyAnimation=getAnim(bodyPart,clip.body);if(!bodyAnimation)return{key:clip.key,segments:[],duration:0};const bodyDuration=animDuration(bodyPart,bodyAnimation)||1;const projectile=buildProjectile(section,meta,clip,bodyPart,bodyAnimation,bodyDuration);const duration=Math.max(bodyDuration,projectile?projectile.spawnTime+projectile.lifetime:0,1);return{key:clip.key,segments:[{clipKey:clip.key,start:0,duration,bodyDuration,projectile,impactTime:clip.isBasicAttack?(projectile?projectile.spawnTime+projectile.normalEnd:nativeProjectileSpawnTime(bodyPart,bodyAnimation.name,clip,bodyDuration)):null,}],duration,};}function buildPlaybackPlan(section,meta,selectedClip){if(!meta||!["attack","full"].includes(selectedClip))return null;const track=computeTrack(section,meta,selectedClip);const attackSegment=track.segments.find((segment)=>segment.clipKey==="attack");if(!attackSegment||attackSegment.impactTime===null)return null;return{cycleDuration:track.duration,impactTime:(attackSegment.start||0)+attackSegment.impactTime,hitPointRate:Number.isFinite(attackSegment.projectile?.hitPointRate)?attackSegment.projectile.hitPointRate:normalizedHitPointRate(meta),};}function normalizedHitPointRate(meta){const raw=finiteNumber(meta?.projectileData?.hitTiming?.normalHitPointRate,NaN);if(!Number.isFinite(raw))return 0.5;return clamp(raw>1?raw/100:raw,0,1);}function installStyles(){if(document.getElementById("ranger-animation-hit-effect-styles"))return;const style=document.createElement("style");style.id="ranger-animation-hit-effect-styles";style.textContent=`
      .ranger-animation-hit-effect-canvas {
        position: absolute;
        inset: 0;
        z-index: 3;
        display: block;
        width: 100%;
        height: 100%;
        pointer-events: none;
      }
    `;document.head.appendChild(style);}function drawSprite(context,image,objectMatrix,imageMatrix,color,originX,originY,scale){const[m00,m01,m10,m11,m02,m12]=objectMatrix;const[i00,i01,i10,i11,i02,i12]=imageMatrix;const centerX=image.width*0.5;const centerY=image.height*0.5;const imageCenterX=i00*centerX+i01*centerY+i02;const imageCenterY=i10*centerX+i11*centerY+i12;const worldCenterX=m00*imageCenterX+m01*imageCenterY+m02;const worldCenterY=m10*imageCenterX+m11*imageCenterY+m12;const f00=m00*i00+m01*i10;const f01=m00*i01+m01*i11;const f10=m10*i00+m11*i10;const f11=m10*i01+m11*i11;const determinant=f00*f11-f01*f10;const localScaleX=Math.hypot(f00,f10);const localScaleY=Math.hypot(f01,f11);const flipX=determinant<0;const angle=flipX?Math.atan2(-f10,-f00):Math.atan2(f10,f00);const alpha=Array.isArray(color)?finiteNumber(color[3],255)/255:1;context.save();context.globalAlpha=clamp(alpha,0,1);context.translate(originX+worldCenterX*scale,originY+worldCenterY*scale);context.rotate(angle);context.scale((flipX?-1:1)*localScaleX*scale,localScaleY*scale);context.drawImage(image,-image.width/2,-image.height/2);context.restore();}async function drawEffectFrame(context,age,x,y,scale){const meta=await loadEffectMeta();const animation=meta?.animations?._all;if(!animation?.frames?.length)return;const segment=(meta.segments||[]).find((item)=>item?.name===HIT_EFFECT_SEGMENT);const start=Math.max(0,Math.trunc(finiteNumber(segment?.start,0)));const end=Math.min(animation.frame_count,Math.trunc(finiteNumber(segment?.end,animation.frame_count)));const frameIndex=start+Math.floor(age*Math.max(1,meta.anim_rate||30));if(frameIndex<start||frameIndex>=end)return;const frame=animation.frames[frameIndex]||[];for(const item of frame){const[,resourceNumber,objectMatrix,color]=item||[];const imageDefinition=meta.images?.[resourceNumber];if(!imageDefinition||!Array.isArray(objectMatrix)||!Array.isArray(imageDefinition.m))continue;const image=await loadEffectImage(imageDefinition.name);if(image)drawSprite(context,image,objectMatrix,imageDefinition.m,color,x,y,scale);}}async function renderFrame(state){const{canvas,section}=state;const baseCanvas=section.querySelector(".ranger-animation-canvas");if(!baseCanvas)return;if(canvas.width!==baseCanvas.width)canvas.width=baseCanvas.width;if(canvas.height!==baseCanvas.height)canvas.height=baseCanvas.height;const context=canvas.getContext("2d");if(!context)return;context.clearRect(0,0,canvas.width,canvas.height);if(!state.plan||!state.meta)return;const elapsed=Math.floor(((performance.now()-state.startedAt)/1000)*NATIVE_ACTION_FPS)/NATIVE_ACTION_FPS;const cycleDuration=Math.max(0,finiteNumber(state.plan.cycleDuration,0));const cycleIndex=cycleDuration>0?Math.floor(elapsed/cycleDuration):0;const time=cycleDuration>0?elapsed-cycleIndex*cycleDuration:elapsed;let impactCycleIndex=cycleIndex;let age=time-state.plan.impactTime;if(age<0&&cycleDuration>0&&elapsed>=cycleDuration){age+=cycleDuration;impactCycleIndex-=1;}if(age<0||age>=HIT_EFFECT_DURATION)return;const scene=window.RangerAnimationSceneBridge?.get(section);if(!scene)return;const target=window.RangerAnimationTargetBridge?.get(section);if(!state.activeEffect||state.activeEffect.cycleIndex!==impactCycleIndex){const targetHeight=Math.max(1,finiteNumber(target?.contentHeight,state.meta?.parts?.body?.canvas?.h||240));const scale=Math.max(0.001,finiteNumber(scene.sceneScale,1));const anchorTop=finiteNumber(target?.anchorTopY,finiteNumber(target?.targetTopY,NaN));const anchorBottom=finiteNumber(target?.anchorBaseY,finiteNumber(target?.targetBaseY,NaN));const hasAnchorBounds=Number.isFinite(anchorTop)&&Number.isFinite(anchorBottom)&&anchorBottom>anchorTop;const x=finiteNumber(target?.anchorX,finiteNumber(target?.targetX,finiteNumber(scene.targetX,canvas.width*0.9)));const y=hasAnchorBounds?anchorBottom-(anchorBottom-anchorTop)*state.plan.hitPointRate:finiteNumber(scene.targetBaseY,canvas.height*0.8)-targetHeight*state.plan.hitPointRate*scale;state.activeEffect={cycleIndex:impactCycleIndex,x,y,scale};}await drawEffectFrame(context,age,state.activeEffect.x,state.activeEffect.y,state.activeEffect.scale);}function startLoop(state){const loop=async()=>{if(!document.contains(state.section)){sectionStates.delete(state.section);return;}if(!state.rendering){state.rendering=true;try{await renderFrame(state);}finally{state.rendering=false;}}state.rafId=requestAnimationFrame(loop);};state.rafId=requestAnimationFrame(loop);}async function rebuildPlan(state,resetTime){if(resetTime)state.startedAt=performance.now();state.activeEffect=null;if(!state.meta)state.meta=await loadUnitMeta(state.unitId);state.plan=buildPlaybackPlan(state.section,state.meta,state.select.value);}function patchSection(section){if(sectionStates.has(section))return;const stack=section.querySelector(".ranger-animation-canvas-stack");const select=section.querySelector(".ranger-animation-select");if(!stack||!select)return;installStyles();const canvas=document.createElement("canvas");canvas.className="ranger-animation-hit-effect-canvas";const baseCanvas=section.querySelector(".ranger-animation-canvas");canvas.width=baseCanvas?.width||640;canvas.height=baseCanvas?.height||360;canvas.setAttribute("aria-hidden","true");stack.appendChild(canvas);const state={section,select,canvas,unitId:text(section.dataset.animationUnitId),meta:null,plan:null,activeEffect:null,startedAt:performance.now(),rendering:false,rafId:0,};sectionStates.set(section,state);select.addEventListener("change",()=>rebuildPlan(state,true));section.addEventListener("ranger-animation-target-change",()=>rebuildPlan(state,true));rebuildPlan(state,false);startLoop(state);}function patchAllSections(){document.querySelectorAll(".ranger-animation-section").forEach(patchSection);}function schedulePatch(){if(patchScheduled)return;patchScheduled=true;queueMicrotask(()=>{patchScheduled=false;patchAllSections();});}const observer=new MutationObserver(schedulePatch);window.addEventListener("DOMContentLoaded",()=>{observer.observe(document.body,{childList:true,subtree:true});patchAllSections();});})();
