/* Ranger detail data is now rendered in the first pass by ranger-recovery.js.
 * This file is intentionally kept as a no-op compatibility shim so older
 * pages that still include it do not perform duplicate post-processing.
 */
(() => {})();
